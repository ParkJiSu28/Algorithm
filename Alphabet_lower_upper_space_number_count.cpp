#include<iostream>
using namespace std;
#include <cstdio>
#include<string>
/*
int main() {

	string str;
	while (getline(cin, str)) {
		int len = str.length();
		int upper = 0;
		int lower = 0;
		int number = 0;
		int space = 0;	
		for (int i = 0; i < len; i++) {
	
			if ('a' <= str[i] && str[i] <= 'z') {
				lower++;
			}
			if ('A' <= str[i] && str[i] <= 'Z') {
				upper++;
			}
			if ('0' <= str[i] && str[i] <= '9') {
				number++;
			}
			if (str[i] == ' ') {
				space++;
			}
		}
		cout << lower << " " << upper << " " << number << " " << space << " "<<endl;
	}
}*/